package cn.itsources.pethome.base.service;
import cn.itsources.pethome.base.result.PageList;
import java.util.List;

public interface IBaseService<T,Q> {
    /**=================================================
     * 方法说明:通过id查询
     * 步骤说明：
     =================================================== **/
    T selectById(Long id);
    /**=================================================
     * 方法说明:查询所有
     * 步骤说明：
     =================================================== **/
    List<T> selectAll();
    /**=================================================
     * 方法说明:插入数据
     * 步骤说明：
     =================================================== **/
    boolean insert(T obj);
    /**=================================================
     * 方法说明:修改数据
     * 步骤说明：
     =================================================== **/
    boolean updateById(T obj);
    /**=================================================
     * 方法说明:删除数据
     * 步骤说明：
     =================================================== **/
    boolean delete(Long id);
    /**=================================================
     * 方法说明:分页查询
     * 步骤说明：
     =================================================== **/
    PageList<T> selectPage(Q query);
    /**=================================================
     * 方法说明:批量删除
     * 步骤说明：
     =================================================== **/
    void deleteByIds(List<Long> ids);
}
